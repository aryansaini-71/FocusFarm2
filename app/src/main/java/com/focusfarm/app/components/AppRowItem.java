package com.focusfarm.app.components;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

public class AppRowItem extends View {

    // TODO: app icon (40dp) + app name + toggle switch

    public AppRowItem(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
}
