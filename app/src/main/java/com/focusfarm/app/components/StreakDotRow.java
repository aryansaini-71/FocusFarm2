package com.focusfarm.app.components;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

public class StreakDotRow extends View {

    // TODO: row of 7 circles (Mon–Sun), filled = completed, empty = missed

    public StreakDotRow(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
}
